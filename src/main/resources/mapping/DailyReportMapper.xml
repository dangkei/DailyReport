<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bgp.report.mapper.DailyReportMapper">
 
    <resultMap id="BaseResultMap" type="com.bgp.report.entity.report">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="userName" />
        <result column="passWord" jdbcType="VARCHAR" property="passWord" />
        <result column="realName" jdbcType="VARCHAR" property="realName" />
    </resultMap>
 
    <select id="Sel" resultType="com.bgp.report.entity.report">
        select * from report where id = #{id}
    </select>
 
	<select id="getUserList" resultType="com.bgp.report.entity.report">
        select <include refid="columns"></include> from report 
    </select>
    
    <select id="count" parameterType="com.bgp.report.entity.report" resultType="int" >
        select count(*) from report 
        <where>
    	<if test="userName != null and !&quot;&quot;.equals(userName.trim())">
	    	and userName like '%' #{userName} '%'
	    </if>
	    <if test="realName != null and !&quot;&quot;.equals(realName.trim())">
	    	and realName like '%' #{realName} '%'
	    </if>
    </where>
    </select>
    
    <select id="getUserListByPage" parameterType="java.util.Map"  resultType="com.bgp.report.entity.DailyReport">
        select * from dailyreport 
    </select>
    
    <sql id="columns">id,userName,passWord,realName</sql>
    
</mapper>